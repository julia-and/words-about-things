<?xml version="1.0" encoding="UTF-8"?>
<feed xmlns="http://www.w3.org/2005/Atom" xml:lang="{{ lang }}">
    <title>{{ config.title }}
    {%- if term %} - {{ term.name }}
    {%- elif section.title %} - {{ section.title }}
    {%- endif -%}
    </title>
    {%- if config.description %}
    <subtitle>{{ config.description }}</subtitle>
    {%- endif %}
    <link rel="self" type="application/atom+xml" href="{{ feed_url | safe }}"/>
    <link rel="alternate" type="text/html" href="
      {%- if section -%}
        {{ section.permalink | escape_xml | safe }}
      {%- else -%}
        {{ config.base_url | escape_xml | safe }}
      {%- endif -%}
    "/>
    <generator uri="https://www.getzola.org/">Zola</generator>
    {% if last_updated is defined %}<updated>{{ last_updated | date(format="%+") }}</updated>{% endif %}
    <id>{{ feed_url | safe }}</id>
    <icon>{{ config.base_url | safe }}/img/favicon.png</icon>
    <logo>{{ config.base_url | safe }}/img/favicon.png</logo>
    {%- for page in pages %}
    <entry xml:lang="{{ page.lang }}">
        <title>{{ page.title }}</title>
        <published>{{ page.date | date(format="%+") }}</published>
        <updated>{{ page.updated | default(value=page.date) | date(format="%+") }}</updated>
        {% for author in page.authors %}
        <author>
          <name>
            {{ author }}
          </name>
        </author>
        {% else %}
        <author>
          <name>
            {%- if config.author -%}
              {{ config.author }}
            {%- else -%}
              Unknown
            {%- endif -%}
          </name>
        </author>
        {% endfor %}
        <link rel="alternate" type="text/html" href="{{ page.permalink | safe }}"/>
        <id>{{ page.permalink | safe }}</id>
        {% if page.summary %}
        {#- print_only shortcode wraps its output in <!--print-only--> sentinels;
            odd-indexed split segments are the print-only markup — drop them #}
        {%- set_global stripped = "" -%}
        {%- for part in page.summary | split(pat="<!--print-only-->") -%}
          {%- if loop.index0 % 2 == 0 -%}{%- set_global stripped = stripped ~ part -%}{%- endif -%}
        {%- endfor %}
        <summary type="html">{{ stripped }}</summary>
        {% else %}
        {%- set_global stripped = "" -%}
        {%- for part in page.content | split(pat="<!--print-only-->") -%}
          {%- if loop.index0 % 2 == 0 -%}{%- set_global stripped = stripped ~ part -%}{%- endif -%}
        {%- endfor %}
        <content type="html" xml:base="{{ page.permalink | escape_xml | safe }}">{{ stripped }}</content>
        {% endif %}
    </entry>
    {%- endfor %}
</feed>
